# Pinned application + tooling dependencies. Requires Python >= 3.11.

# Web stack
fastapi==0.137.2
uvicorn[standard]==0.49.0
jinja2==3.1.6
python-multipart==0.0.32
pydantic==2.13.4
python-dotenv==1.2.2

# Gemini SDK (used by the offline pre-processing pipeline)
google-genai==2.8.0

# HTTP client (pre-processing + tests)
httpx==0.28.1

# Tests
pytest==9.1.0
pytest-asyncio==1.4.0
